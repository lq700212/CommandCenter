# Modbus TCP 通信接口定义文档

**文档版本：** V2.15.17
**适用项目：** 机器人视觉取像系统（含扫码）
**通信模式：** 汇川PLC（主站/Master） ↔ C#上位机（从站/Slave）

## 一、通信连接参数

| 参数项              | 取值          | 备注                            |
| :------------------ | :------------ | :------------------------------ |
| 通信协议            | Modbus TCP    | 基于以太网                      |
| 上位机（从站）IP    | `19.87.6.230` | 固定，请勿变动                  |
| PLC（主站）IP       | `19.87.6.1`   | 由PLC工程师分配确认             |
| 端口号              | `502`         | Modbus TCP默认端口              |
| 从站地址（Unit ID） | `1`           | 待双方确认                      |
| 地址偏移            | **无**        | 实测确认，PLC地址与协议地址一致 |

## 二、寄存器地址映射表

> **寄存器类型说明**：保持寄存器（Holding Register）为16位（Word），可读可写。
> **地址编号说明**：PLC地址与协议地址一致（实测确认无偏移）。
> **设计原则**：定长数据（请求）放在最前面，结果与变长数据（型号序号+型号字符串+SN序列号）放在后面，分类清晰，便于PLC侧顺序配置。
> **V2.14.13 协议升级**：产品型号区拆成"型号序号 + 型号字符串"两部分——`40007` 存**型号对应的序号**
> （现场默认 Z121=1、U171=2，上位机配置 `plc.modelIndexes`），`40008~40012` 存型号 ASCII 字符串
> （原 `40007~40011` 整体后移一位）。PLC 读取型号时**优先用 40007 的序号**区分型号，无需解析字符串。
> **V2.15.17 协议升级**：新增**扫码 SN 序列号区 `40013~40024`**（24 字符，长度可配），编码规则与型号
> 字符串完全一致、无独立握手信号（与产品名称同一套设定）——PLC 在扫码结果 `40004=1` 后读本区即得本件 SN。

### 2.1 地址映射总表

| 顺序 | PLC地址   | 寄存器名称           | 方向（PLC视角） | 取值范围      | 说明                                    |
| :--- | :-------- | :------------------- | :-------------- | :------------ | :-------------------------------------- |
| 1    | **40001** | 扫码请求             | **PLC只写**     | `0` 或 `1`    | `0`=无请求，`1`=请求扫码                |
| 2    | **40002** | 请求拍照上相机       | **PLC只写**     | `0` ~ `255`   | `0`=无请求，`1~255`=对应点位编号        |
| 3    | **40003** | 请求拍照下相机       | **PLC只写**     | `0` ~ `255`   | `0`=无请求，`1~255`=对应点位编号        |
| 4    | **40004** | 扫码结果             | **PLC只读**     | `0`、`1`、`2` | `0`=未扫码/默认，`1`=扫码OK，`2`=扫码NG→**死等人工补录**（读到 2 不复位请求，直到读到 1） |
| 5    | **40005** | 上相机拍照结果       | **PLC只读**     | `0`、`1`、`2`、`3` | `0`=未拍照/复位，`1`=拍照OK，`2`=拍照NG，`3`=点位禁用跳过 |
| 6    | **40006** | 下相机拍照结果       | **PLC只读**     | `0`、`1`、`2`、`3` | 同上相机结果                            |
| 7    | **40007** | 产品型号序号（V2.14.13） | **PLC只读** | `0` ~ `255` | `0`=未配置，`1~255`=型号对应序号（默认 Z121=1、U171=2，上位机配置 `plc.modelIndexes`） |
| 8    | **40008** | 产品型号（字符1-2）  | **PLC只读**     | ASCII字符     | 高字节=字符1，低字节=字符2              |
| 9    | **40009** | 产品型号（字符3-4）  | **PLC只读**     | ASCII字符     | 高字节=字符3，低字节=字符4              |
| 10   | **40010** | 产品型号（字符5-6）  | **PLC只读**     | ASCII字符     | 高字节=字符5，低字节=字符6              |
| 11   | **40011** | 产品型号（字符7-8）  | **PLC只读**     | ASCII字符     | 高字节=字符7，低字节=字符8              |
| 12   | **40012** | 产品型号（字符9-10） | **PLC只读**     | ASCII字符     | 高字节=字符9，低字节=字符10             |
| 13~24 | **40013~40024** | 扫码 SN 序列号（V2.15.17，24字符） | **PLC只读** | ASCII字符 | 编码同型号字符串；扫码OK/补录=本件SN，失败/超时/上电=全0；长度可配（上位机 `plc.scanSerialNumberLen`） |

### 2.2 地址分类速查

#### PLC → 上位机（PLC只写，上位机读取）

| PLC地址 | 寄存器名称     | 取值说明                     |
| :------ | :------------- | :--------------------------- |
| 40001   | 扫码请求       | `0`=无请求，`1`=请求扫码     |
| 40002   | 请求拍照上相机 | `0`=无请求，`1~255`=点位编号 |
| 40003   | 请求拍照下相机 | `0`=无请求，`1~255`=点位编号 |

#### 上位机 → PLC（PLC只读，上位机写入）

| PLC地址     | 寄存器名称         | 取值说明                                  |
| :---------- | :----------------- | :---------------------------------------- |
| 40004       | 扫码结果           | `0`=默认，`1`=OK，`2`=NG（**死等人工补录**，补录后上位机改写成 1，V2.14.33） |
| 40005       | 上相机拍照结果     | `0`=复位，`1`=OK，`2`=NG，`3`=点位禁用跳过 |
| 40006       | 下相机拍照结果     | `0`=复位，`1`=OK，`2`=NG，`3`=点位禁用跳过 |
| 40007       | 产品型号序号（V2.14.13） | `0`=未配置，`1~255`=型号对应序号（默认 Z121=1、U171=2） |
| 40008~40012 | 产品型号（10字符） | ASCII编码，每寄存器存2字符，不足补 `0x00` |
| 40013~40024 | 扫码 SN 序列号（V2.15.17，24字符） | ASCII编码同型号字符串；读到 40004=1 后读取即得本件 SN |

### 2.3 地址布局示意图

```text
地址范围         内容                              方向（PLC视角）
─────────────────────────────────────────────────────────────────
40001 ~ 40003    请求区（扫码/上相机/下相机）       PLC只写
40004 ~ 40006    结果区（扫码/上相机/下相机）       PLC只读
40007            型号序号区（V2.14.13，型号对应序号）PLC只读
40008 ~ 40012    型号区（10字符，5个寄存器）        PLC只读
40013 ~ 40024    SN 序列号区（V2.15.17，24字符，12个寄存器）PLC只读
40025 ~ ...      预留                              可向后扩展
```

> **扩展说明**：若现场 SN 超过 24 字符，调大上位机配置 `plc.scanSerialNumberLen`（SN 区随之向后多占
> 寄存器）；若后续型号长度超过10个字符，调整型号区并同步后移 SN 起始地址——都不影响 `40001`~`40012`
> 的请求/结果/序号地址，双方无需修改已有逻辑。

### 2.4 产品型号（序号 + 字符串）编码说明

- **型号序号（V2.14.13，`40007`）**：每个产品型号对应一个序号，现场默认 `Z121=1`、`U171=2`（上位机
  设置窗体 PLC 区"产品型号配置…"按钮弹窗 **ModelIndexEditForm** 表格维护，V2.14.14 起取代旧
  "型号序号"单行框，存 `plc.modelIndexes` 映射，见 §2.4 型号来源）。PLC **优先用 40007 的序号**
  区分型号，速度快、不受型号名长度影响；`0`=该型号未配序号。
- **型号字符串（`40008` ~ `40012`）**：共5个寄存器，最多可传递 **10个ASCII字符**。
- 每个寄存器存放两个字符：**高字节（Bit 8~15）= 前一字符，低字节（Bit 0~7）= 后一字符**。
- **示例**：型号为 `"Z121"`（序号=1），则：
  - `40007 = 0x0001`（型号序号=1）
  - `40008 = 0x5A31`（'Z'=0x5A，'1'=0x31）
  - `40009 = 0x3231`（'2'=0x32，'1'=0x31）
  - `40010 = 0x0000`（字符串结束）
  - `40011 ~ 40012 = 0x0000`
- 不足10个字符的，在最后一个有效字符后面填 `0x00` 表示字符串结束。
- 若后续发现型号超过10个字符，从 `40013` 开始向后扩展即可。
- **型号来源（V2.8；V2.14.24 入口唯一化）**：**当前型号** = 上位机主界面标题栏型号下拉选择
  （候选=预置 `U171`/`Z121` ∪ 配置 `productModels`）；**型号集合**（增删新型号）统一在设置窗体
   PLC 区"产品型号配置…"弹窗（ModelIndexEditForm）维护——新增型号名保存时自动回流进
  `productModels`（ConfigStore.EnsureModelIndexes 双向对齐），主界面标题栏即可选到；
  **型号序号（V2.14.14）**在 PLC 区"产品型号配置…"按钮
  弹窗（ModelIndexEditForm）里表格维护（V2.14.25 三列=选中勾选列/序号/型号名称，全居中，
   右上角"删除选中"按钮勾选多行批量删 / 点选单选多选删；前几行预载已有映射，确定写回
  `plc.modelIndexes`、取消不影响原配置；取代 V2.14.13 的"型号序号"单行框）。
- **型号写入时机（V2.14.14）**：型号区（40007=序号 + 40008~40012=字符串）不再只等扫码通道推进才写，
  而是三时机写：① **每次扫码推进时写**（原行为）；② **从站建站成功即写**（`PlcService.SetCurrentModel`
  缓存型号，`EnsureConnected` 建站成功即写，覆盖上电/断线重建/热更重建，PLC 不触发扫码也读得到）；
  ③ **主界面切型号立即下发**（`SwitchModel` 更新并重写）。PLC 随时能读到当前型号。
  **型号同时决定相机程序映射**（`modelStationPrograms`，见 `docs/CommandCenter.md` §4）：切型号即切换
   对应的"点位→相机程序号"表，触发相机时按新型号发 `PW,nnn` 切程序。

### 2.5 扫码 SN 序列号编码说明（V2.15.17）

- **SN 区（`40013` ~ `40024`）**：默认 12 个寄存器 = **最多 24 个 ASCII 字符**（长度可配，上位机
  配置 `plc.scanSerialNumberLen`）。
- 编码规则与产品型号字符串**完全一致**：每寄存器存放两个字符——**高字节（Bit 8~15）= 前一字符，
  低字节（Bit 0~7）= 后一字符**；不足补 `0x00`、以 `0x00` 作字符串结束符。
- **无独立握手信号（与产品名称同一套设定）**：上位机在写扫码结果的同时已把本件 SN 写好——
  PLC 读到 `40004 = 1`（扫码 OK）后读 SN 区即得**本件**序列号。
- **失败/超时/上电时 SN 区为全 0**；扫码结果读到 `2`（死等人工补录）期间 SN 区也是全 0，
  补录完成后上位机**先写补录 SN、再把 40004 覆盖成 1**——PLC 读到 1 后读到的必是本件有效 SN。
- **示例**：本件 SN 为 `"AB20260820001"`（12 字符），则：
  - `40013 = 0x4142`（'A'=0x41，'B'=0x42）
  - `40014 ~ 40018` = '2','0','2','6','0','8','2','0','0','0' 两两打包
  - `40019 = 0x3100`（'1'=0x31，尾补 0x00=字符串结束）
  - `40020 ~ 40024 = 0x0000`
- **写入时机（与产品型号同一套设定）**：① 扫码 OK / 人工补录覆盖时写实际 SN；② 扫码失败/超时整区清 0；
  ③ 上位机从站断线重建后自动回写最近一次 SN（防 PLC 重连后读到空）；④ 上位机上电初始化整区清 0。

### 2.6 SN 区读取与重置规则（V2.15.17，联调必读）

**【读取规则】**
- **何时读**：PLC 读到扫码结果 `40004 = 1` 后再读 SN 区——上位机保证"SN 先于结果落地"，
  读到 1 时 SN 必已就绪；`40004 = 0` 或 `2` 时 SN 区内容不代表本件（见下方生命周期）。
- **怎么读**：功能码 03 从 `40013` 起一次性连续读 12 个寄存器（默认 24 字符），按
  "高字节=前一字符、低字节=后一字符"拼字节串，以第一个 `0x00` 截断；全 `0x00` = 本件无有效 SN。
- **读几次**：建议在本件流程内读一次并缓存到 PLC 内部寄存器；重复读同一件结果一致，无害。

**【所有权——PLC 只读，绝不写】**
- SN 区由上位机**独占写入与清零**，PLC 对该区**只读**：不写、不清零、不复位，读完也**无需任何
  回执动作**（不像请求寄存器那样要写 0 应答）。

**【生命周期与残留语义——最重要的一条】**
- 本件 SN 自 `40004 = 1` 起有效；此后**即使 PLC 已复位扫码请求、上位机已把 40004 归 0，
  SN 区仍保持上一件的值**——这是设计行为（配合断线重建回写，防数据丢失），不是脏数据。
- **判断"新件到达"的唯一依据是 `40001`（请求）/`40004`（结果）的边沿，绝不能用"SN 区内容
  变化了"当新件信号**——下一件扫码完成前 SN 区不会变。
- 上一件 SN 被"覆盖/清除"只有三种可能：① 下一件扫码 OK 写入新 SN；② 下一件扫码失败/超时整区
  清 0（同时结果写 2 死等补录）；③ 上位机重启/从站重建（先整区清 0，建站成功后回写最近一次缓存）。
- **上电初始化**：上位机从站每次建站成功先把 SN 区整区清 0（日志可见"+ SN 区"），随后回写当前
  型号与最近一次 SN 缓存；PLC 侧重启后照常按"读到 40004=1 才取 SN"使用即可，无需额外配合。

## 三、功能码约定

| 操作                      | 功能码（十进制） | 说明                                                         |
| :------------------------ | :--------------- | :----------------------------------------------------------- |
| PLC读取 `40004` ~ `40024` | 03（0x03）       | 读保持寄存器（V2.15.17 起 SN 区并入连续读取范围）             |
| PLC写入 `40001` ~ `40003` | 16（0x10）       | 写多个保持寄存器（批量） （也支持单个写入功能码06，批量更高效） |

> **说明**：PLC侧使用功能码16（写多个寄存器）批量写入请求数据，符合标准Modbus规范，上位机NModbus库可自动解析，无需额外适配。

## 四、完整交互时序

text

```
PLC（主站）                                                                                         C#上位机（从站）
     │                                                                                                      │
     │ 阶段一：扫码                                                                                           │
     │                                                                                                      │
     │ ① 机器人带扫码枪运动到位                                                                                 │
     │ 写 40001 = 1（请求扫码）                                                                                │
     │─────────────────────────────────────────────────────────────────────────────────────────────────────>│
     │                                                                                                      │
      │                                                                                                      │ ② 检测到 40001 = 1
      │                                                                                                      │    → 与扫码枪TCP/IP通信，获取产品SN
      │                                                                                                      │    → 解析获取产品型号字符串（如"Z121"）
      │                                                                                                      │
       │                                                                                                      │ ③ 扫码完成，上报结果、型号与SN
       │                                                                                                      │    写 40007 = 型号序号（V2.14.13，Z121=1/U171=2）
       │                                                                                                      │    写 40008~40012 = 产品型号字符串
       │                                                                                                      │    写 40013~40024 = 本件 SN 序列号（V2.15.17；失败/超时写全 0）
       │                                                                                                      │    写 40004 = 扫码结果（1=OK / 2=NG·死等补录）
       │  <─────────────────────────────────────────────────────────────────────────────────────────────────────│
       │                                                                                                        │
       │ ④ 检测到 40004 ≠ 0（扫码有结果）                                                                           │
       │    读 40007 获取型号序号（优先用序号区分型号）                                                              │
       │    读 40008~40012 获取型号字符串                                                                          │
       │    读 40013~40024 获取本件 SN 序列号（V2.15.17；读到 1 时 SN 必已就绪）                                     │
      │    执行后续动作                                                                                          │
      │    写 40001 = 0（复位扫码请求）                                                                           │
     │───────────────────────────────────────────────────────────────────────────────────────────────────────>│
     │                                                                                                        │
     │                                                                                                        │ ⑤ 检测到 40001 = 0
     │  <─────────────────────────────────────────────────────────────────────────────────────────────────────│ 写 40004 = 0（复位扫码结果）
     │                                                                                                      │
     │ 阶段二：上相机拍照                                                                                      │
     │                                                                                                      │
     │ ⑥ 机器人带相机运动到上相机拍照位                                                                           │
     │    写 40002 = 点位编号（如点位3 → 写 3）                                                                  │
     │─────────────────────────────────────────────────────────────────────────────────────────────────────>│
     │                                                                                                      │
     │                                                                                                      │ ⑦ 检测到 40002 ≠ 0
     │                                                                                                      │    → 读取点位编号（40002的值）
     │                                                                                                      │    → 触发上相机取像
     │                                                                                                      │    → 图片保存至对应点位文件夹
     │                                                                                                      │    → 在界面对应点位窗口显示图片
     │                                                                                                      │
     │                                                                                                      │ ⑧ 上相机拍照完成，上报结果
     │  <─────────────────────────────────────────────────────────────────────────────────────────────────────│ 写 40005 = 拍照结果（1=OK / 2=NG）
     │                                                                                                      │
     │ ⑨ 检测到 40005 ≠ 0                                                                                  │
     │    执行后续动作                                                                                      │
     │    写 40002 = 0（复位上相机拍照请求）                                                                │
     │─────────────────────────────────────────────────────────────────────────────────────────────────────>│
     │                                                                                                      │
     │                                                                                                      │ ⑩ 检测到 40002 = 0
     │  <─────────────────────────────────────────────────────────────────────────────────────────────────────│ 写 40005 = 0（复位上相机拍照结果）
     │                                                                                                      │
     │ 阶段三：下相机拍照                                                                                   │
     │                                                                                                      │
     │ ⑪ 机器人带相机运动到下相机拍照位                                                                     │
     │    写 40003 = 点位编号（如点位5 → 写 5）                                                             │
     │─────────────────────────────────────────────────────────────────────────────────────────────────────>│
     │                                                                                                      │
     │                                                                                                      │ ⑫ 检测到 40003 ≠ 0
     │                                                                                                      │    → 读取点位编号（40003的值）
     │                                                                                                      │    → 触发下相机取像
     │                                                                                                      │    → 图片保存至对应点位文件夹
     │                                                                                                      │    → 在界面对应点位窗口显示图片
     │                                                                                                      │
     │                                                                                                      │ ⑬ 下相机拍照完成，上报结果
     │  <─────────────────────────────────────────────────────────────────────────────────────────────────────│ 写 40006 = 拍照结果（1=OK / 2=NG）
     │                                                                                                      │
     │ ⑭ 检测到 40006 ≠ 0                                                                                  │
     │    执行后续动作（走下一工位）                                                                        │
     │    写 40003 = 0（复位下相机拍照请求）                                                                │
     │─────────────────────────────────────────────────────────────────────────────────────────────────────>│
     │                                                                                                      │
     │                                                                                                      │ ⑮ 检测到 40003 = 0
     │  <─────────────────────────────────────────────────────────────────────────────────────────────────────│ 写 40006 = 0（复位下相机拍照结果）
     │                                                                                                      │
     ▼                                                                                                      ▼
  准备下一轮循环                                                                                          等待下一轮触发
```



## 五、异常处理约定

| 异常情况                      | 处理方式                                                     |
| :---------------------------- | :----------------------------------------------------------- |
| 扫码超时（如5秒未返回SN）     | 上位机写 `40004 = 2`（NG）+ **SN 区清 0**（V2.15.17），界面报警"扫码超时"；PLC **死等人工补录**（V2.14.33），补录完成后上位机写补录 SN 并把结果覆盖成 1，流程继续 |
| 上相机拍照超时（如5秒未完成） | 上位机写 `40005 = 2`（NG），界面报警"上相机拍照超时"，流程继续 |
| 下相机拍照超时（如5秒未完成） | 上位机写 `40006 = 2`（NG），界面报警"下相机拍照超时"，流程继续 |
| 通信中断                      | 上位机界面显示"通信中断"，PLC侧建议做超时保护（如3秒未收到反馈则报警）；上位机从站重建成功后自动回写当前型号与最近一次 SN，无需 PLC 额外配合 |

## 六、PLC侧配置要求

1. 请将上述 `40001` ~ `40024` 共24个寄存器，按**连续地址**映射到PLC内部可读写的地址区域。
   ⚠️ **`40013` ~ `40024` 已被 SN 序列号区占用（V2.15.17），PLC 梯形图不得挪作他用**；
   后续如需为第 3 台及以上相机新增拍照通道，协商请求/结果地址时**必须避开本区段**
   （从 `40025` 之后选址，并与上位机相机表配置保持一致）。
2. 请确保PLC的Modbus TCP主站功能已开启，目标从站IP = `19.87.6.230`，端口 = `502`，Unit ID = `1`。
3. **地址无偏移**：实测确认，PLC写入地址与上位机读取地址一致，无需做 `-40001` 换算。
4. **功能码16写入**：PLC可使用功能码16（0x10）一次性写入 `40001` ~ `40003` 等多个请求值，上位机NModbus库可正常解析。
5. 建议PLC程序中，对请求信号的置位加入防抖延时（建议100ms），避免机器人抖动导致误触发。
6. **寄存器使用说明**：
   - `40001`：PLC写入扫码请求，上位机读取
   - `40002`：PLC写入上相机拍照请求（点位编号），上位机读取
   - `40003`：PLC写入下相机拍照请求（点位编号），上位机读取
   - `40004`：上位机写入扫码结果，PLC读取（读到 2 = 死等人工补录，V2.14.33）
   - `40005`：上位机写入上相机拍照结果，PLC读取
   - `40006`：上位机写入下相机拍照结果，PLC读取
   - `40007`：上位机写入**型号序号**（V2.14.13，默认 Z121=1、U171=2，`0`=未配序号），PLC读取
   - `40008` ~ `40012`：上位机写入型号字符串（最多10字符），PLC读取
   - `40013` ~ `40024`：上位机写入**本件 SN 序列号**（V2.15.17，24字符 ASCII，编码同型号字符串；
     读到 40004=1 后读取；失败/超时/上电为全 0；补录覆盖时已同步写入补录 SN），PLC读取
7. **型号识别建议（V2.14.13）**：PLC 侧优先读 `40007` 的序号判断当前型号（数值比较最快最稳），
   型号字符串 `40008~40012` 作辅助信息（如显示/日志）；未配序号的型号（40007=0）才回退解析字符串。
8. **SN 序列号读取建议（V2.15.17）**：在扫码结果读到 1 后一次性读 `40013` 起 12 个寄存器（24 字符，
   `0x00` 结束）即得本件 SN；SN 区无独立握手信号、跟随 40004 结果走（与产品型号同一套设定）。
   **SN 区 PLC 只读、读完无需回执动作；40004 归 0 后 SN 保持上一件值属正常，判断新件只看
   40001/40004 边沿**（详见 §2.6）。
9. **字符串结束符**：PLC读取型号/SN时，以 `0x00` 作为字符串结束标志，0x00之后的字节忽略。
10. **扩展预留**：若现场 SN 超过 24 字符或型号超过10个字符，与上位机同步调整对应区长度/起始地址即可，
    不影响已有地址。

## 七、上位机代码示例（C#）

### 7.1 读取PLC请求

```csharp
// 读取扫码请求（40001）
ushort[] scanReq = master.ReadHoldingRegisters(40001, 1);
bool isScanRequested = scanReq[0] == 1;

// 读取上相机拍照请求（40002）
ushort[] camUpReq = master.ReadHoldingRegisters(40002, 1);
int camUpPoint = camUpReq[0];  // 0=无请求，>0=点位编号

// 读取下相机拍照请求（40003）
ushort[] camDownReq = master.ReadHoldingRegisters(40003, 1);
int camDownPoint = camDownReq[0];  // 0=无请求，>0=点位编号
```

### 7.2 写入扫码结果（40004）

```csharp
// 扫码OK
master.WriteSingleRegister(40004, 1);
// 扫码NG
master.WriteSingleRegister(40004, 2);
// 复位
master.WriteSingleRegister(40004, 0);
```

### 7.3 写入拍照结果（40005 / 40006）

```csharp
// 上相机OK
master.WriteSingleRegister(40005, 1);
// 上相机NG
master.WriteSingleRegister(40005, 2);
// 下相机OK
master.WriteSingleRegister(40006, 1);
// 下相机NG
master.WriteSingleRegister(40006, 2);
// 复位
master.WriteSingleRegister(40005, 0);
master.WriteSingleRegister(40006, 0);
```

### 7.4 写入产品型号（40007=型号序号 + 40008~40012=型号字符串）

```csharp
/// <summary>
/// 写产品型号（V2.14.13 格式）：先写 40007 型号序号（查 plc.modelIndexes 映射，型号没配写 0），
/// 再写 40008~40012 型号 ASCII 字符串（最多10个字符）。
/// 调用时机（V2.14.14 三时机）：每次扫码推进 / 从站建站成功 / 主界面切型号立即下发。
/// </summary>
public void WriteProductModel(string model, int modelIndex)
{
    // ① 型号序号 → 40007（V2.14.13）
    master.WriteSingleRegister(40007, (ushort)modelIndex);

    // ② 型号字符串 → 40008~40012
    byte[] bytes = new byte[10];
    byte[] modelBytes = Encoding.ASCII.GetBytes(model);
    int len = Math.Min(modelBytes.Length, 10);
    Array.Copy(modelBytes, bytes, len);
    // 末尾自动补0（已初始化全0）

    List<ushort> registers = new List<ushort>();
    for (int i = 0; i < 10; i += 2)
    {
        ushort value = (ushort)((bytes[i] << 8) | bytes[i + 1]);
        registers.Add(value);
    }

    master.WriteMultipleRegisters(40008, registers.ToArray());
}

// 调用示例：型号 "Z121"（序号=1）
WriteProductModel("Z121", 1);
// 写入后：
// 40007 = 0x0001（型号序号=1）
// 40008 = 0x5A31 ('Z','1')
// 40009 = 0x3231 ('2','1')
// 40010 = 0x0000 (字符串结束)
// 40011~40012 = 0x0000
```

### 7.5 PLC侧读取型号（序号 + 字符串，参考）

```csharp
// PLC读取 40007（型号序号）+ 40008~40012（型号字符串）共6个寄存器
ushort[] data = master.ReadHoldingRegisters(40007, 6);
int modelIndex = data[0]; // 40007 = 型号序号（V2.14.13，优先用序号区分型号）

// 40008~40012 转10字节
byte[] bytes = new byte[10];
for (int i = 0; i < 5; i++)
{
    bytes[i * 2] = (byte)(data[i + 1] >> 8);
    bytes[i * 2 + 1] = (byte)(data[i + 1] & 0xFF);
}

// 截取到第一个 '\0' 结束
int end = Array.IndexOf(bytes, (byte)0);
if (end < 0) end = bytes.Length;
string model = Encoding.ASCII.GetString(bytes, 0, end);
```

### 7.6 PLC侧读取本件 SN 序列号（40013~40024，V2.15.17 参考）

```csharp
// 在扫码结果 40004 读到 1（扫码 OK）后读取 SN 区：默认 12 个寄存器 = 24 字符
ushort[] sn = master.ReadHoldingRegisters(40013, 12);

byte[] snBytes = new byte[24];
for (int i = 0; i < 12; i++)
{
    snBytes[i * 2]     = (byte)(sn[i] >> 8);      // 高字节=前一字符
    snBytes[i * 2 + 1] = (byte)(sn[i] & 0xFF);    // 低字节=后一字符
}

// 以第一个 '\0' 为字符串结束符；全 0 = 本件无有效 SN（失败/超时/上电）
int snEnd = Array.IndexOf(snBytes, (byte)0);
if (snEnd < 0) snEnd = snBytes.Length;
string serialNumber = Encoding.ASCII.GetString(snBytes, 0, snEnd);
```

**请确认以上配置，尤其PLC的IP地址、Unit ID及地址映射关系，如有异议请及时沟通。**
**确认无误后，双方按此文档开发联调。**

------

*文档提供方：上位机软件开发工程师*
*日期：2026-08-25（V2.15.17 新增 SN 序列号区 40013~40024）*